package com.vpk.sprachninja.di

import android.content.Context

/**
 * A manual dependency injection container that is created in the Application class.
 * It will hold and provide instances of repositories, data sources, and use cases.
 */
class AppContainer(private val context: Context) {
    // Dependencies will be added here in later steps
}